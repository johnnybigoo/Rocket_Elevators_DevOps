document.addEventListener('turbolinks:load', function (event) {
  if (typeof gtag === 'function') {
    gtag('config', "<%= Rails.application.credentials.dig(:GOOGLE_ANALYTICS_KEY) %>", {
      'page_location': event.data.url
    })
  }
})